// Function that takes a string and return a new string with all vowels removed
// Initial solution

public class Troll {
    public static String disemvowel(String str) {
        StringBuilder newStr = new StringBuilder("");
        char temp;
        int j = 0;
        
        for(int i = 0; i < str.length(); i++){
          switch(str.charAt(i)){
            case 'a':
              break;
            case 'e':
              break;
            case 'i':
              break;
            case 'o':
              break;
            case 'u':
              break;
            case 'A':
              break;
            case 'E':
              break;
            case 'I':
              break;
            case 'O':
              break;
            case 'U':
              break;
            default:
              temp = str.charAt(i);
              newStr.append(temp);
              j++;
              break;
          }
        }
        return newStr.toString();
    }
}

// Best solution
// Submitted by: _Anonymous_,EAnoch

// Uses (?!)[*****] to change both lowercase and uppercase of *****

/*
public class Troll{
  public static String disemvowel(String Z){
    return Z.replaceAll("(?!)[aeiou]", "");
  }
}

*/
